class Square extends Rectangle {
    constructor(width, color, left, top) {
        super(width, width, color, left, top);
    }
}

// Parent class, biggest contsructions, with most variety. And child elements only need some components of it

// const sq1 = new Square(300, "blue", 100, 500);

// sq1.draw();
